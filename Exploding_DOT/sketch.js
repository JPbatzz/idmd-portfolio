var x = 250;
var y = 250;
var w = 50;
function setup() {
  createCanvas(500, 500);
  background(255);
  noStroke();
}
function draw() {
  background(255);
  fill(w/2,0,0);
  ellipse(x, y, w, w);
  distance = dist(x, y, mouseX, mouseY);
  if (distance < w / 2) {
    //   V - changes speed
    w += 2;
  } else {
    //   V - changes original width
    w = 5;
    
    
if (w <= 500) {
  } else {  
  background(random(255),random(255),random(255))

  

}
 
  
  }}